🌌 An Astrology Web (Group E)
โปรเจกต์เว็บพยากรณ์ชะตาชีวิต พัฒนาด้วย Next.js 15+ (Turbopack), Tailwind CSS v4, และ Prisma โดยรันอยู่บนระบบ Docker ทั้งหมด

🛠️ สิ่งที่ต้องมีก่อนเริ่ม (Prerequisites)
Docker Desktop ติดตั้งและรันให้เรียบร้อย

Node.js (แนะนำ v20 ขึ้นไป สำหรับรันคำสั่งจัดการแพ็กเกจในเครื่อง)

🚀 ขั้นตอนการติดตั้งและรันโปรเจกต์ (First-time Setup)
หากคุณ Clone โปรเจกต์นี้ไปเป็นครั้งแรก ให้ทำตามขั้นตอนดังนี้:

1. เตรียมไฟล์ Environment
    สร้างไฟล์ .env ไว้ที่โฟลเดอร์ grop_E/app/ และระบุค่าเชื่อมต่อฐานข้อมูล
    Code snippet :
    DATABASE_URL="mysql://user:password@mysql:3306/astrology_db"

2. ติดตั้ง Dependencies (เลือกทำอย่างใดอย่างหนึ่ง)
    หากต้องการให้ VS Code ไม่ขึ้นตัวแดงและใช้ Auto-complete ได้ ให้รันคำสั่งนี้ที่เครื่อง:

    Bash
    cd grop_E/app
    npm install

3. รันระบบด้วย Docker
    เราใช้ Docker ในการ Build และรันทั้ง App, Database และ phpMyAdmin:
    
    สำหรับ Development:
        Bash
        docker-compose up -d --build

    สำหรับ Production (Build แบบ Optimized):
        Bash
        docker-compose -f docker-compose.prod.yml up -d --build
    
4. การจัดการฐานข้อมูล (Prisma)
    หลังจากคอนเทนเนอร์รันแล้ว หากมีการแก้ไข Database Schema ให้รันคำสั่งนี้ภายในคอนเทนเนอร์:
    Bash
    docker-compose exec app npx prisma db push

📬 ช่องทางการเข้าใช้งาน
Next.js App: http://localhost:3000

phpMyAdmin: http://localhost:8080 (สำหรับจัดการ Database ผ่าน UI)

📁 โครงสร้างโฟลเดอร์ที่สำคัญ
เพื่อให้ Next.js และ Tailwind v4 ทำงานได้ถูกต้อง โครงสร้างต้องเป็นดังนี้:

src/app/ : เก็บ Layout, Page และ CSS หลักของระบบ

prisma/ : เก็บไฟล์ Schema ของฐานข้อมูล

Dockerfile.prod : ไฟล์ตั้งค่าการ Build สำหรับ Production

⚠️ หมายเหตุการแก้ไขปัญหา (Troubleshooting)
Tailwind v4: โปรเจกต์นี้ใช้ @import "tailwindcss"; ใน globals.css และใช้ @tailwindcss/postcss ในการ Build

Network Error: หาก npm install ใน Docker ล้มเหลว ให้ลองรีสตาร์ท Docker Desktop แล้ว Build ใหม่อีกครั้งโดยเพิ่ม --no-cache